# Normalize text files to LF in repo (Windows working copies may still be CRLF depending on tools)
* text=auto eol=lf

# PowerShell scripts: keep as text, normalize to LF in repo
*.ps1 text eol=lf
*.psm1 text eol=lf
*.psd1 text eol=lf

# YAML
*.yml text eol=lf
*.yaml text eol=lf
